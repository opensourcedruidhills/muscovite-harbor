-- Deploy: cargo_decomposition.delivery_units
-- Generated by muscomp catalog pipeline

BEGIN;

SET search_path TO cargo_decomposition, public;

CREATE TABLE IF NOT EXISTS delivery_units (
    id UUID NOT NULL,
    tracking_number tracking_number NOT NULL,
    destination TEXT NOT NULL,
    carrier TEXT NOT NULL,
    dispatched_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by UUID,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by UUID,
    delivery_unit_id UUID NOT NULL,
    PRIMARY KEY (id)
);

COMMIT;
