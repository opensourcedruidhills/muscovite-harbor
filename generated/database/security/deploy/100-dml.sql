-- DML functions for schema security
-- Generated by muscomp — do not edit

BEGIN;

SET search_path TO security, public;

-- DML: security.users

CREATE OR REPLACE FUNCTION security.insert_users(
    p_id UUID DEFAULT uuidv7(),
    p_username TEXT,
    p_email TEXT,
    p_password_hash TEXT,
    p_is_active BOOLEAN,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.users AS $$
    INSERT INTO security.users(id, username, email, password_hash, is_active, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_username, p_email, p_password_hash, p_is_active, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_users_by_id(
    p_id UUID
) RETURNS SETOF security.users AS $$
    SELECT * FROM security.users WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_users(
    p_id UUID,
    p_username TEXT,
    p_email TEXT,
    p_password_hash TEXT,
    p_is_active BOOLEAN,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.users AS $$
    UPDATE security.users
    SET username = p_username,
        email = p_email,
        password_hash = p_password_hash,
        is_active = p_is_active,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_users(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.users WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: security.user_roles

CREATE OR REPLACE FUNCTION security.insert_user_roles(
    p_id UUID DEFAULT uuidv7(),
    p_user_id TEXT,
    p_role_id TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.user_roles AS $$
    INSERT INTO security.user_roles(id, user_id, role_id, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_user_id, p_role_id, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_user_roles_by_id(
    p_id UUID
) RETURNS SETOF security.user_roles AS $$
    SELECT * FROM security.user_roles WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_user_roles(
    p_id UUID,
    p_user_id TEXT,
    p_role_id TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.user_roles AS $$
    UPDATE security.user_roles
    SET user_id = p_user_id,
        role_id = p_role_id,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_user_roles(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.user_roles WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: security.roles

CREATE OR REPLACE FUNCTION security.insert_roles(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT,
    p_description TEXT DEFAULT NULL,
    p_permissions TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.roles AS $$
    INSERT INTO security.roles(id, name, description, permissions, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_name, p_description, p_permissions, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_roles_by_id(
    p_id UUID
) RETURNS SETOF security.roles AS $$
    SELECT * FROM security.roles WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_roles(
    p_id UUID,
    p_name TEXT,
    p_description TEXT DEFAULT NULL,
    p_permissions TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.roles AS $$
    UPDATE security.roles
    SET name = p_name,
        description = p_description,
        permissions = p_permissions,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_roles(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.roles WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: security.permissions

CREATE OR REPLACE FUNCTION security.insert_permissions(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT,
    p_resource TEXT,
    p_action TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.permissions AS $$
    INSERT INTO security.permissions(id, name, resource, action, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_name, p_resource, p_action, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_permissions_by_id(
    p_id UUID
) RETURNS SETOF security.permissions AS $$
    SELECT * FROM security.permissions WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_permissions(
    p_id UUID,
    p_name TEXT,
    p_resource TEXT,
    p_action TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.permissions AS $$
    UPDATE security.permissions
    SET name = p_name,
        resource = p_resource,
        action = p_action,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_permissions(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.permissions WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: security.access_control_entries

CREATE OR REPLACE FUNCTION security.insert_access_control_entries(
    p_id UUID DEFAULT uuidv7(),
    p_principal TEXT,
    p_resource TEXT,
    p_permission TEXT,
    p_effect TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.access_control_entries AS $$
    INSERT INTO security.access_control_entries(id, principal, resource, permission, effect, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_principal, p_resource, p_permission, p_effect, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_access_control_entries_by_id(
    p_id UUID
) RETURNS SETOF security.access_control_entries AS $$
    SELECT * FROM security.access_control_entries WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_access_control_entries(
    p_id UUID,
    p_principal TEXT,
    p_resource TEXT,
    p_permission TEXT,
    p_effect TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.access_control_entries AS $$
    UPDATE security.access_control_entries
    SET principal = p_principal,
        resource = p_resource,
        permission = p_permission,
        effect = p_effect,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_access_control_entries(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.access_control_entries WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: security.audit_log_entries

CREATE OR REPLACE FUNCTION security.insert_audit_log_entries(
    p_id UUID DEFAULT uuidv7(),
    p_timestamp TIMESTAMPTZ,
    p_actor TEXT,
    p_action TEXT,
    p_resource TEXT,
    p_details TEXT DEFAULT NULL,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS security.audit_log_entries AS $$
    INSERT INTO security.audit_log_entries(id, timestamp, actor, action, resource, details, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_timestamp, p_actor, p_action, p_resource, p_details, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.select_audit_log_entries_by_id(
    p_id UUID
) RETURNS SETOF security.audit_log_entries AS $$
    SELECT * FROM security.audit_log_entries WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION security.update_audit_log_entries(
    p_id UUID,
    p_timestamp TIMESTAMPTZ,
    p_actor TEXT,
    p_action TEXT,
    p_resource TEXT,
    p_details TEXT DEFAULT NULL,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS security.audit_log_entries AS $$
    UPDATE security.audit_log_entries
    SET timestamp = p_timestamp,
        actor = p_actor,
        action = p_action,
        resource = p_resource,
        details = p_details,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION security.delete_audit_log_entries(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM security.audit_log_entries WHERE id = p_id;
$$ LANGUAGE SQL;

COMMIT;
