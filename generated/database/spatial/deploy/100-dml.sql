-- DML functions for schema spatial
-- Generated by muscomp — do not edit

BEGIN;

SET search_path TO spatial, public;

-- DML: spatial.coordinates

CREATE OR REPLACE FUNCTION spatial.insert_coordinates(
    p_id UUID DEFAULT uuidv7(),
    p_latitude NUMERIC,
    p_longitude NUMERIC,
    p_altitude NUMERIC DEFAULT NULL,
    p_srid INTEGER DEFAULT NULL,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.coordinates AS $$
    INSERT INTO spatial.coordinates(id, latitude, longitude, altitude, srid, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_latitude, p_longitude, p_altitude, p_srid, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.select_coordinates_by_id(
    p_id UUID
) RETURNS SETOF spatial.coordinates AS $$
    SELECT * FROM spatial.coordinates WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION spatial.update_coordinates(
    p_id UUID,
    p_latitude NUMERIC,
    p_longitude NUMERIC,
    p_altitude NUMERIC DEFAULT NULL,
    p_srid INTEGER DEFAULT NULL,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.coordinates AS $$
    UPDATE spatial.coordinates
    SET latitude = p_latitude,
        longitude = p_longitude,
        altitude = p_altitude,
        srid = p_srid,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.delete_coordinates(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM spatial.coordinates WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: spatial.regions

CREATE OR REPLACE FUNCTION spatial.insert_regions(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT,
    p_boundary TEXT,
    p_center TEXT DEFAULT NULL,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.regions AS $$
    INSERT INTO spatial.regions(id, name, boundary, center, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_name, p_boundary, p_center, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.select_regions_by_id(
    p_id UUID
) RETURNS SETOF spatial.regions AS $$
    SELECT * FROM spatial.regions WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION spatial.update_regions(
    p_id UUID,
    p_name TEXT,
    p_boundary TEXT,
    p_center TEXT DEFAULT NULL,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.regions AS $$
    UPDATE spatial.regions
    SET name = p_name,
        boundary = p_boundary,
        center = p_center,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.delete_regions(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM spatial.regions WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: spatial.spatial_projections

CREATE OR REPLACE FUNCTION spatial.insert_spatial_projections(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT,
    p_srid INTEGER,
    p_definition TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.spatial_projections AS $$
    INSERT INTO spatial.spatial_projections(id, name, srid, definition, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_name, p_srid, p_definition, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.select_spatial_projections_by_id(
    p_id UUID
) RETURNS SETOF spatial.spatial_projections AS $$
    SELECT * FROM spatial.spatial_projections WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION spatial.update_spatial_projections(
    p_id UUID,
    p_name TEXT,
    p_srid INTEGER,
    p_definition TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS spatial.spatial_projections AS $$
    UPDATE spatial.spatial_projections
    SET name = p_name,
        srid = p_srid,
        definition = p_definition,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION spatial.delete_spatial_projections(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM spatial.spatial_projections WHERE id = p_id;
$$ LANGUAGE SQL;

COMMIT;
