-- Deploy: vessel_traffic.tug_bookings
-- Generated by muscomp catalog pipeline

BEGIN;

SET search_path TO vessel_traffic, public;

CREATE TABLE IF NOT EXISTS tug_bookings (
    id UUID NOT NULL,
    voyage_id UUID NOT NULL,
    tug_name TEXT NOT NULL,
    bollard_pull_t NUMERIC NOT NULL,
    is_confirmed BOOLEAN NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    created_by UUID,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_by UUID,
    tug_booking_id UUID NOT NULL,
    PRIMARY KEY (id)
);

COMMIT;
