-- DML functions for schema passenger_terminal
-- Generated by muscomp — do not edit

BEGIN;

SET search_path TO passenger_terminal, public;

-- DML: passenger_terminal.passengers

CREATE OR REPLACE FUNCTION passenger_terminal.insert_passengers(
    p_id UUID DEFAULT uuidv7(),
    p_booking_ref booking_reference,
    p_passenger_type TEXT,
    p_voyage_id UUID,
    p_status TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL,
    p_status_id UUID
) RETURNS passenger_terminal.passengers AS $$
    INSERT INTO passenger_terminal.passengers(id, booking_ref, passenger_type, voyage_id, status, created_at, created_by, updated_at, updated_by, status_id)
    VALUES (p_id, p_booking_ref, p_passenger_type, p_voyage_id, p_status, p_created_at, p_created_by, p_updated_at, p_updated_by, p_status_id)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_passengers_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.passengers AS $$
    SELECT * FROM passenger_terminal.passengers WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_passengers(
    p_id UUID,
    p_booking_ref booking_reference,
    p_passenger_type TEXT,
    p_voyage_id UUID,
    p_status TEXT,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL,
    p_status_id UUID
) RETURNS passenger_terminal.passengers AS $$
    UPDATE passenger_terminal.passengers
    SET booking_ref = p_booking_ref,
        passenger_type = p_passenger_type,
        voyage_id = p_voyage_id,
        status = p_status,
        created_by = p_created_by,
        updated_by = p_updated_by,
        status_id = p_status_id,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_passengers(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.passengers WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.gates

CREATE OR REPLACE FUNCTION passenger_terminal.insert_gates(
    p_id UUID DEFAULT uuidv7(),
    p_code gate_code,
    p_name TEXT,
    p_capacity INTEGER,
    p_status TEXT,
    p_voyage_id UUID DEFAULT NULL,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL,
    p_status_id UUID
) RETURNS passenger_terminal.gates AS $$
    INSERT INTO passenger_terminal.gates(id, code, name, capacity, status, voyage_id, created_at, created_by, updated_at, updated_by, status_id)
    VALUES (p_id, p_code, p_name, p_capacity, p_status, p_voyage_id, p_created_at, p_created_by, p_updated_at, p_updated_by, p_status_id)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_gates_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.gates AS $$
    SELECT * FROM passenger_terminal.gates WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_gates(
    p_id UUID,
    p_code gate_code,
    p_name TEXT,
    p_capacity INTEGER,
    p_status TEXT,
    p_voyage_id UUID DEFAULT NULL,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL,
    p_status_id UUID
) RETURNS passenger_terminal.gates AS $$
    UPDATE passenger_terminal.gates
    SET code = p_code,
        name = p_name,
        capacity = p_capacity,
        status = p_status,
        voyage_id = p_voyage_id,
        created_by = p_created_by,
        updated_by = p_updated_by,
        status_id = p_status_id,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_gates(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.gates WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.boarding_passes

CREATE OR REPLACE FUNCTION passenger_terminal.insert_boarding_passes(
    p_id UUID DEFAULT uuidv7(),
    p_passenger_id UUID,
    p_gate_id UUID DEFAULT NULL,
    p_boarding_group TEXT,
    p_seat_number TEXT DEFAULT NULL,
    p_issued_at TIMESTAMPTZ,
    p_scanned_at TIMESTAMPTZ DEFAULT NULL,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_created_by UUID DEFAULT NULL,
    p_updated_at TIMESTAMPTZ DEFAULT NOW(),
    p_updated_by UUID DEFAULT NULL
) RETURNS passenger_terminal.boarding_passes AS $$
    INSERT INTO passenger_terminal.boarding_passes(id, passenger_id, gate_id, boarding_group, seat_number, issued_at, scanned_at, created_at, created_by, updated_at, updated_by)
    VALUES (p_id, p_passenger_id, p_gate_id, p_boarding_group, p_seat_number, p_issued_at, p_scanned_at, p_created_at, p_created_by, p_updated_at, p_updated_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_boarding_passes_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.boarding_passes AS $$
    SELECT * FROM passenger_terminal.boarding_passes WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_boarding_passes(
    p_id UUID,
    p_passenger_id UUID,
    p_gate_id UUID DEFAULT NULL,
    p_boarding_group TEXT,
    p_seat_number TEXT DEFAULT NULL,
    p_issued_at TIMESTAMPTZ,
    p_scanned_at TIMESTAMPTZ DEFAULT NULL,
    p_created_by UUID DEFAULT NULL,
    p_updated_by UUID DEFAULT NULL
) RETURNS passenger_terminal.boarding_passes AS $$
    UPDATE passenger_terminal.boarding_passes
    SET passenger_id = p_passenger_id,
        gate_id = p_gate_id,
        boarding_group = p_boarding_group,
        seat_number = p_seat_number,
        issued_at = p_issued_at,
        scanned_at = p_scanned_at,
        created_by = p_created_by,
        updated_by = p_updated_by,
        updated_at = NOW()
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_boarding_passes(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.boarding_passes WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.outbox

CREATE OR REPLACE FUNCTION passenger_terminal.insert_outbox(
    p_id UUID DEFAULT uuidv7(),
    p_aggregate_type TEXT,
    p_aggregate_id UUID,
    p_event_type TEXT,
    p_payload JSONB,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_retry_count INTEGER DEFAULT 0,
    p_processed_at TIMESTAMPTZ DEFAULT NULL
) RETURNS passenger_terminal.outbox AS $$
    INSERT INTO passenger_terminal.outbox(id, aggregate_type, aggregate_id, event_type, payload, created_at, retry_count, processed_at)
    VALUES (p_id, p_aggregate_type, p_aggregate_id, p_event_type, p_payload, p_created_at, p_retry_count, p_processed_at)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_outbox_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.outbox AS $$
    SELECT * FROM passenger_terminal.outbox WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_outbox(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.outbox WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.dead_letter_queue

CREATE OR REPLACE FUNCTION passenger_terminal.insert_dead_letter_queue(
    p_id UUID DEFAULT uuidv7(),
    p_original_event_id UUID,
    p_event_type TEXT,
    p_payload JSONB,
    p_error_message TEXT,
    p_retry_count INTEGER DEFAULT 0,
    p_created_at TIMESTAMPTZ DEFAULT NOW()
) RETURNS passenger_terminal.dead_letter_queue AS $$
    INSERT INTO passenger_terminal.dead_letter_queue(id, original_event_id, event_type, payload, error_message, retry_count, created_at)
    VALUES (p_id, p_original_event_id, p_event_type, p_payload, p_error_message, p_retry_count, p_created_at)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_dead_letter_queue_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.dead_letter_queue AS $$
    SELECT * FROM passenger_terminal.dead_letter_queue WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_dead_letter_queue(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.dead_letter_queue WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.idempotency_keys

CREATE OR REPLACE FUNCTION passenger_terminal.insert_idempotency_keys(
    p_key TEXT,
    p_created_at TIMESTAMPTZ DEFAULT NOW(),
    p_expires_at TIMESTAMPTZ
) RETURNS passenger_terminal.idempotency_keys AS $$
    INSERT INTO passenger_terminal.idempotency_keys(key, created_at, expires_at)
    VALUES (p_key, p_created_at, p_expires_at)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_idempotency_keys_by_id(
    p_key TEXT
) RETURNS SETOF passenger_terminal.idempotency_keys AS $$
    SELECT * FROM passenger_terminal.idempotency_keys WHERE key = p_key;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_idempotency_keys(
    p_key TEXT,
    p_expires_at TIMESTAMPTZ
) RETURNS passenger_terminal.idempotency_keys AS $$
    UPDATE passenger_terminal.idempotency_keys
    SET expires_at = p_expires_at
    WHERE key = p_key
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_idempotency_keys(
    p_key TEXT
) RETURNS void AS $$
    DELETE FROM passenger_terminal.idempotency_keys WHERE key = p_key;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.gate_status_states

CREATE OR REPLACE FUNCTION passenger_terminal.insert_gate_status_states(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT
) RETURNS passenger_terminal.gate_status_states AS $$
    INSERT INTO passenger_terminal.gate_status_states(id, name)
    VALUES (p_id, p_name)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_gate_status_states_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.gate_status_states AS $$
    SELECT * FROM passenger_terminal.gate_status_states WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_gate_status_states(
    p_id UUID,
    p_name TEXT
) RETURNS passenger_terminal.gate_status_states AS $$
    UPDATE passenger_terminal.gate_status_states
    SET name = p_name
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_gate_status_states(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.gate_status_states WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.gate_status_transitions

CREATE OR REPLACE FUNCTION passenger_terminal.insert_gate_status_transitions(
    p_id UUID DEFAULT uuidv7(),
    p_entity_id UUID,
    p_from_state TEXT,
    p_to_state TEXT,
    p_transitioned_at TIMESTAMPTZ DEFAULT NOW(),
    p_transitioned_by UUID DEFAULT NULL
) RETURNS passenger_terminal.gate_status_transitions AS $$
    INSERT INTO passenger_terminal.gate_status_transitions(id, entity_id, from_state, to_state, transitioned_at, transitioned_by)
    VALUES (p_id, p_entity_id, p_from_state, p_to_state, p_transitioned_at, p_transitioned_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_gate_status_transitions_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.gate_status_transitions AS $$
    SELECT * FROM passenger_terminal.gate_status_transitions WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_gate_status_transitions(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.gate_status_transitions WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.passenger_status_states

CREATE OR REPLACE FUNCTION passenger_terminal.insert_passenger_status_states(
    p_id UUID DEFAULT uuidv7(),
    p_name TEXT
) RETURNS passenger_terminal.passenger_status_states AS $$
    INSERT INTO passenger_terminal.passenger_status_states(id, name)
    VALUES (p_id, p_name)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_passenger_status_states_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.passenger_status_states AS $$
    SELECT * FROM passenger_terminal.passenger_status_states WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.update_passenger_status_states(
    p_id UUID,
    p_name TEXT
) RETURNS passenger_terminal.passenger_status_states AS $$
    UPDATE passenger_terminal.passenger_status_states
    SET name = p_name
    WHERE id = p_id
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_passenger_status_states(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.passenger_status_states WHERE id = p_id;
$$ LANGUAGE SQL;

-- DML: passenger_terminal.passenger_status_transitions

CREATE OR REPLACE FUNCTION passenger_terminal.insert_passenger_status_transitions(
    p_id UUID DEFAULT uuidv7(),
    p_entity_id UUID,
    p_from_state TEXT,
    p_to_state TEXT,
    p_transitioned_at TIMESTAMPTZ DEFAULT NOW(),
    p_transitioned_by UUID DEFAULT NULL
) RETURNS passenger_terminal.passenger_status_transitions AS $$
    INSERT INTO passenger_terminal.passenger_status_transitions(id, entity_id, from_state, to_state, transitioned_at, transitioned_by)
    VALUES (p_id, p_entity_id, p_from_state, p_to_state, p_transitioned_at, p_transitioned_by)
    RETURNING *;
$$ LANGUAGE SQL;

CREATE OR REPLACE FUNCTION passenger_terminal.select_passenger_status_transitions_by_id(
    p_id UUID
) RETURNS SETOF passenger_terminal.passenger_status_transitions AS $$
    SELECT * FROM passenger_terminal.passenger_status_transitions WHERE id = p_id;
$$ LANGUAGE SQL STABLE;

CREATE OR REPLACE FUNCTION passenger_terminal.delete_passenger_status_transitions(
    p_id UUID
) RETURNS void AS $$
    DELETE FROM passenger_terminal.passenger_status_transitions WHERE id = p_id;
$$ LANGUAGE SQL;

COMMIT;
