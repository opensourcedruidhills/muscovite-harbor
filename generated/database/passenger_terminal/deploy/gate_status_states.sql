-- Deploy: passenger_terminal.gate_status_states
-- Generated by muscomp catalog pipeline

BEGIN;

SET search_path TO passenger_terminal, public;

CREATE TABLE IF NOT EXISTS gate_status_states (
    id UUID NOT NULL DEFAULT uuidv7(),
    name TEXT NOT NULL,
    PRIMARY KEY (id)
);

COMMIT;
