# Generated by muscomp. DO NOT EDIT.
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        build_type: [Debug, Release]

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: muscovite_harbor
          POSTGRES_USER: muscovite_harbor
          POSTGRES_PASSWORD: ci_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v4

      - name: Install Conan
        run: pip install conan

      - name: Conan install
        run: |
          conan profile detect
          conan install . --build=missing

      - name: CMake configure
        run: cmake --preset ${{ matrix.build_type == 'Debug' && 'debug' || 'release' }}

      - name: Build
        run: cmake --build --preset ${{ matrix.build_type == 'Debug' && 'debug' || 'release' }}

      - name: Test
        run: ctest --preset ${{ matrix.build_type == 'Debug' && 'debug' || 'release' }} --output-on-failure
        env:
          DATABASE_URL: postgresql://muscovite_harbor:ci_password@localhost:5432/muscovite_harbor
