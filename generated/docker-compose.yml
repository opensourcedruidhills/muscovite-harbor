# Generated by muscomp. DO NOT EDIT.
# Infrastructure services for MuscoviteHarbor

services:
  postgres:
    image: postgres:16-alpine
    container_name: muscovite_harbor-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-muscovite_harbor}
      POSTGRES_USER: ${POSTGRES_USER:-muscovite_harbor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./docker/init-schemas.sql:/docker-entrypoint-initdb.d/01-schemas.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-muscovite_harbor}"]
      interval: 5s
      timeout: 5s
      retries: 5

  nats:
    image: nats:2-alpine
    container_name: muscovite_harbor-nats
    ports:
      - "${NATS_PORT:-4222}:4222"
      - "${NATS_MONITOR_PORT:-8222}:8222"
    command: ["--js", "--http_port", "8222"]
    healthcheck:
      test: ["CMD", "nats", "server", "check", "connection"]
      interval: 5s
      timeout: 3s
      retries: 3

volumes:
  pgdata:
