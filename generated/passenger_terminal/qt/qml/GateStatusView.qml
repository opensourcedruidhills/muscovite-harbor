// This file was generated by muscomp. DO NOT EDIT.

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Page {
    id: root
    title: qsTr("GateStatus State Machine")

    property string currentState: "CLOSED"

    signal transitionRequested(string event)

    ColumnLayout {
        anchors.fill: parent
        anchors.margins: 16
        spacing: 12

        Label {
            text: qsTr("GateStatus")
            font.pixelSize: 18
            font.bold: true
        }

        Label {
            text: qsTr("Current State: %1").arg(root.currentState)
            font.pixelSize: 14
        }

        Label {
            text: qsTr("States")
            font.bold: true
        }

        Flow {
            Layout.fillWidth: true
            spacing: 8

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "CLOSED" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("CLOSED")
                    color: root.currentState === "CLOSED" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "CHECK_IN_OPEN" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("CheckInOpen")
                    color: root.currentState === "CHECK_IN_OPEN" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "BOARDING" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("BOARDING")
                    color: root.currentState === "BOARDING" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "FINAL_CALL" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("FinalCall")
                    color: root.currentState === "FINAL_CALL" ? "white" : "black"
                }
            }

        }

        Label {
            text: qsTr("Available Transitions")
            font.bold: true
        }

        Flow {
            Layout.fillWidth: true
            spacing: 8

            Button {
                text: qsTr("OpenCheckIn → CheckInOpen")
                visible: root.currentState === "CLOSED"
                onClicked: root.transitionRequested("OpenCheckIn")
            }

            Button {
                text: qsTr("StartBoarding → BOARDING")
                visible: root.currentState === "CHECK_IN_OPEN"
                onClicked: root.transitionRequested("StartBoarding")
            }

            Button {
                text: qsTr("AnnounceFinalCall → FinalCall")
                visible: root.currentState === "BOARDING"
                onClicked: root.transitionRequested("AnnounceFinalCall")
            }

            Button {
                text: qsTr("CloseGate → CLOSED")
                visible: root.currentState === "FINAL_CALL"
                onClicked: root.transitionRequested("CloseGate")
            }

            Button {
                text: qsTr("CancelBoarding → CLOSED")
                visible: root.currentState === "CHECK_IN_OPEN"
                onClicked: root.transitionRequested("CancelBoarding")
            }

        }

        Item { Layout.fillHeight: true }
    }
}

