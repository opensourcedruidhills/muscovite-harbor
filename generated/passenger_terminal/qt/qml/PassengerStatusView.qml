// This file was generated by muscomp. DO NOT EDIT.

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Page {
    id: root
    title: qsTr("PassengerStatus State Machine")

    property string currentState: "CHECKED_IN"

    signal transitionRequested(string event)

    ColumnLayout {
        anchors.fill: parent
        anchors.margins: 16
        spacing: 12

        Label {
            text: qsTr("PassengerStatus")
            font.pixelSize: 18
            font.bold: true
        }

        Label {
            text: qsTr("Current State: %1").arg(root.currentState)
            font.pixelSize: 14
        }

        Label {
            text: qsTr("States")
            font.bold: true
        }

        Flow {
            Layout.fillWidth: true
            spacing: 8

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "CHECKED_IN" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("CheckedIn")
                    color: root.currentState === "CHECKED_IN" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "SECURITY_CLEARED" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("SecurityCleared")
                    color: root.currentState === "SECURITY_CLEARED" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "BOARDED" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("BOARDED")
                    color: root.currentState === "BOARDED" ? "white" : "black"
                }
            }

            Rectangle {
                width: 120; height: 40; radius: 4
                color: root.currentState === "DISEMBARKED" ? "#2196f3" : "#e0e0e0"
                border.width: 1
                border.color: "#bdbdbd"

                Label {
                    anchors.centerIn: parent
                    text: qsTr("DISEMBARKED")
                    color: root.currentState === "DISEMBARKED" ? "white" : "black"
                }
            }

        }

        Label {
            text: qsTr("Available Transitions")
            font.bold: true
        }

        Flow {
            Layout.fillWidth: true
            spacing: 8

            Button {
                text: qsTr("PassSecurity → SecurityCleared")
                visible: root.currentState === "CHECKED_IN"
                onClicked: root.transitionRequested("PassSecurity")
            }

            Button {
                text: qsTr("BoardVessel → BOARDED")
                visible: root.currentState === "SECURITY_CLEARED"
                onClicked: root.transitionRequested("BoardVessel")
            }

            Button {
                text: qsTr("Disembark → DISEMBARKED")
                visible: root.currentState === "BOARDED"
                onClicked: root.transitionRequested("Disembark")
            }

        }

        Item { Layout.fillHeight: true }
    }
}

