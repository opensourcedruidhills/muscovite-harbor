// SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Muscovite-Commercial
// Copyright (c) 2025-2026 Johannes Lochmann
//
// This software is dual-licensed:
//  - GNU Affero General Public License v3 (AGPL-3.0), see LICENSE-AGPL
//  - Commercial license, contact muscovite-licensing@johanneslochmann.net
//
// Generated by Muscovite - DO NOT EDIT
// Any changes will be overwritten on regeneration.
// This file was generated by muscomp. DO NOT EDIT.

#include "widgets/voyage_status_widget.hpp"

#include <QHBoxLayout>

namespace muscovite_harbor::widgets {

VoyageStatusWidget::VoyageStatusWidget(QWidget* parent)
    : QWidget{parent}
    , current_state_{QStringLiteral("ANNOUNCED")} {
    setupUi();
    updateTransitionButtons();
}

void VoyageStatusWidget::setupUi() {
    auto* layout = new QVBoxLayout{this};

    auto* header = new QLabel{QStringLiteral("VoyageStatus State Machine"), this};
    header->setStyleSheet(QStringLiteral("font-size: 16px; font-weight: bold;"));
    layout->addWidget(header);

    state_label_ = new QLabel{this};
    state_label_->setText(QStringLiteral("Current State: ") + current_state_);
    layout->addWidget(state_label_);

    transition_container_ = new QWidget{this};
    transition_container_->setLayout(new QHBoxLayout{transition_container_});
    layout->addWidget(transition_container_);

    auto* history_label = new QLabel{QStringLiteral("State History"), this};
    history_label->setStyleSheet(QStringLiteral("font-weight: bold;"));
    layout->addWidget(history_label);

    history_list_ = new QListWidget{this};
    history_list_->addItem(QStringLiteral("Initial: ANNOUNCED"));
    layout->addWidget(history_list_);
}

void VoyageStatusWidget::updateTransitionButtons() {
    auto* layout = transition_container_->layout();
    while (auto* item = layout->takeAt(0)) {
        delete item->widget();
        delete item;
    }

    if (current_state_ == QStringLiteral("ANNOUNCED")) {
        auto* btn = new QPushButton{QStringLiteral("OrderPilot \u2192 PilotOrdered"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("OrderPilot"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("PILOT_ORDERED")) {
        auto* btn = new QPushButton{QStringLiteral("VesselApproaching \u2192 APPROACHING"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("VesselApproaching"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("APPROACHING")) {
        auto* btn = new QPushButton{QStringLiteral("BeginBerthing \u2192 BERTHING"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("BeginBerthing"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("BERTHING")) {
        auto* btn = new QPushButton{QStringLiteral("VesselMoored \u2192 MOORED"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("VesselMoored"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("MOORED")) {
        auto* btn = new QPushButton{QStringLiteral("BeginCargoOps \u2192 CargoOps"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("BeginCargoOps"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("CARGO_OPS")) {
        auto* btn = new QPushButton{QStringLiteral("CargoComplete \u2192 DeparturePrep"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("CargoComplete"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("DEPARTURE_PREP")) {
        auto* btn = new QPushButton{QStringLiteral("CastOff \u2192 UNMOORED"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("CastOff"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("UNMOORED")) {
        auto* btn = new QPushButton{QStringLiteral("VesselDeparted \u2192 DEPARTED"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("VesselDeparted"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("APPROACHING")) {
        auto* btn = new QPushButton{QStringLiteral("AbortApproach \u2192 ANNOUNCED"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("AbortApproach"));
        });
        layout->addWidget(btn);
    }
    if (current_state_ == QStringLiteral("BERTHING")) {
        auto* btn = new QPushButton{QStringLiteral("AbortBerthing \u2192 APPROACHING"), transition_container_};
        QObject::connect(btn, &QPushButton::clicked, this, [this]() {
            Q_EMIT transitionRequested(QStringLiteral("AbortBerthing"));
        });
        layout->addWidget(btn);
    }
}

void VoyageStatusWidget::setCurrentState(const QString& state) {
    current_state_ = state;
    state_label_->setText(QStringLiteral("Current State: ") + state);
    history_list_->addItem(state);
    updateTransitionButtons();
}

auto VoyageStatusWidget::currentState() const -> QString {
    return current_state_;
}

} // namespace muscovite_harbor::widgets

