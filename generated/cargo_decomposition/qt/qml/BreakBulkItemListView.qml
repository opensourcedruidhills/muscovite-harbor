// This file was generated by muscomp. DO NOT EDIT.

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Page {
    id: root
    title: qsTr("BreakBulkItems")

    property int currentPage: 1
    property int totalPages: 1
    property string filterText: ""

    signal itemClicked(string id)
    signal refreshRequested()

    ColumnLayout {
        anchors.fill: parent
        anchors.margins: 8

        RowLayout {
            Layout.fillWidth: true

            TextField {
                id: searchField
                placeholderText: qsTr("Search BreakBulkItem...")
                Layout.fillWidth: true
                onTextChanged: root.filterText = text
            }

            Button {
                text: qsTr("Refresh")
                onClicked: root.refreshRequested()
            }
        }

        ListView {
            id: listView
            Layout.fillWidth: true
            Layout.fillHeight: true
            clip: true

            delegate: ItemDelegate {
                width: listView.width
                contentItem: RowLayout {
                    spacing: 8
                    Label { text: model.id || ""; Layout.fillWidth: true }
                    Label { text: model.containerId || ""; Layout.fillWidth: true }
                    Label { text: model.itemType || ""; Layout.fillWidth: true }
                    Label { text: model.weightKg || ""; Layout.fillWidth: true }
                    Label { text: model.requiresCrane || ""; Layout.fillWidth: true }
                }
                onClicked: root.itemClicked(model.id || "")
            }

            Label {
                anchors.centerIn: parent
                text: qsTr("No BreakBulkItem items found")
                visible: listView.count === 0
            }
        }

        RowLayout {
            Layout.fillWidth: true
            Layout.alignment: Qt.AlignHCenter

            Button {
                text: qsTr("Previous")
                enabled: root.currentPage > 1
                onClicked: root.currentPage--
            }

            Label {
                text: qsTr("Page %1 of %2").arg(root.currentPage).arg(root.totalPages)
            }

            Button {
                text: qsTr("Next")
                enabled: root.currentPage < root.totalPages
                onClicked: root.currentPage++
            }
        }
    }
}

