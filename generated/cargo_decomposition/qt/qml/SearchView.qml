// This file was generated by muscomp. DO NOT EDIT.

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Page {
    id: root
    title: qsTr("Search")

    property string searchQuery: ""
    property string selectedEntityType: ""

    signal resultClicked(string entityType, string entityId)

    ColumnLayout {
        anchors.fill: parent
        anchors.margins: 8

        TextField {
            id: globalSearchField
            placeholderText: qsTr("Search across all entities...")
            Layout.fillWidth: true
            onTextChanged: root.searchQuery = text
        }

        RowLayout {
            Layout.fillWidth: true
            spacing: 4

            Label { text: qsTr("Filter:") }

            Button {
                text: qsTr("All")
                flat: root.selectedEntityType !== ""
                highlighted: root.selectedEntityType === ""
                onClicked: root.selectedEntityType = ""
            }
            Button {
                text: qsTr("Pallet")
                flat: root.selectedEntityType !== "Pallet"
                highlighted: root.selectedEntityType === "Pallet"
                onClicked: root.selectedEntityType = "Pallet"
            }
            Button {
                text: qsTr("Parcel")
                flat: root.selectedEntityType !== "Parcel"
                highlighted: root.selectedEntityType === "Parcel"
                onClicked: root.selectedEntityType = "Parcel"
            }
            Button {
                text: qsTr("DeliveryUnit")
                flat: root.selectedEntityType !== "DeliveryUnit"
                highlighted: root.selectedEntityType === "DeliveryUnit"
                onClicked: root.selectedEntityType = "DeliveryUnit"
            }
            Button {
                text: qsTr("BreakBulkItem")
                flat: root.selectedEntityType !== "BreakBulkItem"
                highlighted: root.selectedEntityType === "BreakBulkItem"
                onClicked: root.selectedEntityType = "BreakBulkItem"
            }
        }

        ListView {
            id: resultList
            Layout.fillWidth: true
            Layout.fillHeight: true
            clip: true

            delegate: ItemDelegate {
                width: resultList.width
                contentItem: RowLayout {
                    spacing: 8

                    Rectangle {
                        width: 80; height: 24; radius: 4
                        color: "#e3f2fd"
                        Label {
                            anchors.centerIn: parent
                            text: model.entityType || ""
                            font.pixelSize: 11
                        }
                    }

                    Label {
                        text: model.displayText || ""
                        Layout.fillWidth: true
                    }
                }
                onClicked: root.resultClicked(model.entityType || "", model.entityId || "")
            }

            Label {
                anchors.centerIn: parent
                text: root.searchQuery === "" ? qsTr("Enter a search term") : qsTr("No results found")
                visible: resultList.count === 0
            }
        }
    }
}

